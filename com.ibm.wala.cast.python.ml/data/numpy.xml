<?xml version="1.0" ?>
<!DOCTYPE summary-spec>
<summary-spec>
  <classloader name="PythonLoader">
    <class name="numpy" allocatable="true">
      <method name="import" static="true" descriptor="()Lnumpy;">
        <!-- The `Lnumpy` module object must stay this method's FIRST allocation: the dtype-argument resolver reaches it as `NewSiteReference.make(0, NUMPY_TYPE)` in this node to test whether a candidate token is one of the fields below. -->
        <new def="x" class="Lnumpy"/>
        <!-- https://numpy.org/doc/stable/reference/arrays.scalars.html -->
        <!-- The scalar types are type objects, so each name has two roles: passed as `dtype=`, it names a dtype; called, it constructs a rank-0 array of that dtype (`np.float64(2.0)`). Both roles are served by a single allocation per name, whose class is the constructor's (dispatching `NpScalarType`) rather than `Ltensorflow/dtypes/DType`. The dtype role survives the class change because the dtype-argument resolver matches a token by its identity in the module field's points-to set, not by its allocated class; the class-keyed arms that back that resolution up read `NumpyTypes.SCALAR_TYPE_TO_DTYPE`. wala/ML#827. -->
        <new def="float32" class="Lnumpy/float32"/>
        <putfield class="LRoot" field="float32" fieldType="LRoot" ref="x" value="float32"/>
        <new def="float64" class="Lnumpy/float64"/>
        <putfield class="LRoot" field="float64" fieldType="LRoot" ref="x" value="float64"/>
        <new def="int32" class="Lnumpy/int32"/>
        <putfield class="LRoot" field="int32" fieldType="LRoot" ref="x" value="int32"/>
        <new def="int64" class="Lnumpy/int64"/>
        <putfield class="LRoot" field="int64" fieldType="LRoot" ref="x" value="int64"/>
        <new def="uint8" class="Lnumpy/uint8"/>
        <putfield class="LRoot" field="uint8" fieldType="LRoot" ref="x" value="uint8"/>
        <!-- `np.bool` is not a scalar type: it was an alias of the Python builtin, deprecated in NumPy 1.20 and removed in 1.24, so it stays a bare dtype token. Its scalar-type sibling is `np.bool_`. -->
        <new def="bool" class="Ltensorflow/dtypes/DType"/>
        <putfield class="LRoot" field="bool" fieldType="LRoot" ref="x" value="bool"/>
        <new def="bool_" class="Lnumpy/bool_"/>
        <putfield class="LRoot" field="bool_" fieldType="LRoot" ref="x" value="bool_"/>
        <!-- https://numpy.org/doc/stable/reference/random/legacy.html -->
        <!-- `np.random`, the legacy module-level random surface, modeled as a plain module object (the pattern `tensorflow.xml` uses for `tf.random`) whose fields hold the per-function classes. The module-alias form `rng = np.random` reaches the same fields, since the alias copies the reference. wala/ML#827. -->
        <new def="random" class="Lobject"/>
        <putfield class="LRoot" field="random" fieldType="LRoot" ref="x" value="random"/>
        <new def="randn_fn" class="Lnumpy/random/randn"/>
        <putfield class="LRoot" field="randn" fieldType="LRoot" ref="random" value="randn_fn"/>
        <new def="rand_fn" class="Lnumpy/random/rand"/>
        <putfield class="LRoot" field="rand" fieldType="LRoot" ref="random" value="rand_fn"/>
        <new def="np_normal_fn" class="Lnumpy/random/normal"/>
        <putfield class="LRoot" field="normal" fieldType="LRoot" ref="random" value="np_normal_fn"/>
        <new def="np_uniform_fn" class="Lnumpy/random/uniform"/>
        <putfield class="LRoot" field="uniform" fieldType="LRoot" ref="random" value="np_uniform_fn"/>
        <new def="np_permutation_fn" class="Lnumpy/random/permutation"/>
        <putfield class="LRoot" field="permutation" fieldType="LRoot" ref="random" value="np_permutation_fn"/>
        <new def="seed_fn" class="Lnumpy/random/seed"/>
        <putfield class="LRoot" field="seed" fieldType="LRoot" ref="random" value="seed_fn"/>
        <new def="random_state_fn" class="Lnumpy/random/RandomState"/>
        <putfield class="LRoot" field="RandomState" fieldType="LRoot" ref="random" value="random_state_fn"/>
        <new def="ndarray_fn" class="Lnumpy/ndarray_constructor"/>
        <putfield class="LRoot" field="ndarray" fieldType="LRoot" ref="x" value="ndarray_fn"/>
        <new def="array_fn" class="Lnumpy/array"/>
        <putfield class="LRoot" field="array" fieldType="LRoot" ref="x" value="array_fn"/>
        <new def="ones_fn" class="Lnumpy/ones"/>
        <putfield class="LRoot" field="ones" fieldType="LRoot" ref="x" value="ones_fn"/>
        <new def="zeros_fn" class="Lnumpy/zeros"/>
        <putfield class="LRoot" field="zeros" fieldType="LRoot" ref="x" value="zeros_fn"/>
        <new def="reshape_fn" class="Lnumpy/reshape"/>
        <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="x" value="reshape_fn"/>
        <new def="np_transpose_fn" class="Lnumpy/transpose"/>
        <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="x" value="np_transpose_fn"/>
        <new def="eye_fn" class="Lnumpy/eye"/>
        <putfield class="LRoot" field="eye" fieldType="LRoot" ref="x" value="eye_fn"/>
        <new def="unique_fn" class="Lnumpy/unique"/>
        <putfield class="LRoot" field="unique" fieldType="LRoot" ref="x" value="unique_fn"/>
        <return value="x"/>
      </method>
    </class>
    <package name="numpy">
      <!-- https://numpy.org/doc/stable/reference/arrays.scalars.html -->
      <!-- The scalar type constructors. Calling one coerces its argument to a rank-0 array of that type, so each `do` allocates an ndarray whose shape and dtype the paired `NpScalarType` fixes; the argument contributes neither axis. The same allocation doubles as the `dtype=` token — see the module method above. wala/ML#827. -->
      <class name="float32" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="2" paramNames="self value">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <class name="float64" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="2" paramNames="self value">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <class name="int32" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="2" paramNames="self value">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <class name="int64" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="2" paramNames="self value">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <class name="uint8" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="2" paramNames="self value">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <class name="bool_" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="2" paramNames="self value">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <!-- https://numpy.org/doc/stable/reference/generated/numpy.ndarray.html -->
      <!-- The `np.ndarray(shape, dtype, ...)` constructor: an uninitialized array of the given shape and dtype, so the typing model is the `zeros` allocator's (shape from arg 0, dtype from the `dtype` argument) and the dispatch reuses `NpZeros`. wala/ML#775. -->
      <class name="ndarray_constructor" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="4" paramNames="self shape dtype buffer">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <!-- https://numpy.org/doc/stable/reference/generated/numpy.array.html -->
      <!-- Modeled as a class-per-function so that `np.array` resolves to this class via a field lookup on the numpy module, and the invocation dispatches to its `do` method. The returned ndarray preserves shape from arg 0 and takes dtype from arg 1; see `NpArray`. -->
      <class name="array" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="3" paramNames="self x dtype">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <!-- https://numpy.org/doc/stable/reference/generated/numpy.ones.html -->
      <!-- Modeled as a class-per-function (same pattern as `array`) so that `np.ones` resolves to this class via a field lookup on the numpy module, and the invocation dispatches to its `do` method. The returned ndarray takes its shape from arg 0 (a shape tuple) and its dtype from the `dtype` argument; see `NpOnes`. -->
      <class name="ones" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="3" paramNames="self shape dtype">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <!-- https://numpy.org/doc/stable/reference/generated/numpy.zeros.html -->
      <!-- Modeled as a class-per-function (same pattern as `array`) so that `np.zeros` resolves to this class via a field lookup on the numpy module, and the invocation dispatches to its `do` method. The returned ndarray takes its shape from arg 0 (a shape tuple) and its dtype from the `dtype` argument; see `NpZeros`. -->
      <class name="zeros" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="3" paramNames="self shape dtype">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <!-- https://numpy.org/doc/stable/reference/generated/numpy.eye.html -->
      <!-- Modeled as a class-per-function (same pattern as `zeros`) so that `np.eye` resolves to this class via a field lookup on the numpy module, and the invocation dispatches to its `do` method. The returned identity matrix takes its shape from the `n`/`m` arguments and its dtype from the `dtype` argument (float64 default); the `k` diagonal offset does not affect the type. See `NpEye`; wala/ML#797. -->
      <class name="eye" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="5" paramNames="self n m k dtype">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <!-- Must be declared `allocatable="true"` so that `<new class="Lnumpy/ndarray"/>` in the various array-producing `do()` methods (both here and in `tensorflow.xml`) resolves to a concrete `InstanceKey` in the heap model. Without this declaration, `HeapModel.getInstanceKeyForAllocation` returns `null`, `getInvariantContents` returns empty, and `visitReturn` never adds the iKey to the caller-side `PointerKey`. Same pattern as the wala/WALA#1889 fix for `tensorflow/python/ops/variables/Variable`. The class deliberately has no method body because it is only referenced as a `<new>` target, not invoked directly. -->
      <class name="ndarray" allocatable="true"></class>
      <!-- https://numpy.org/doc/stable/reference/generated/numpy.unique.html -->
      <!-- `np.unique(ar, return_index, return_inverse, return_counts, axis)` modeled as the flag form the corpus destructures: a tuple whose slot 0 is the unique values (dtype preserved from `ar`, flattened to rank 1) and whose slots 1-3 are the index, inverse, and counts outputs (positional indices, always int64 regardless of the input dtype). Each slot is produced by its own class (the `load_data` per-output pattern) so tuple-field reads dispatch per-slot generators; see `NpUniqueValues`/`NpUniqueIndices`. The flagless call's runtime result is the bare values array rather than a tuple; this model returns the tuple regardless, a deliberate approximation scoped to the destructuring form. wala/ML#799. -->
      <class name="unique" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="6" paramNames="self ar return_index return_inverse return_counts axis">
          <new def="values_fn" class="Lnumpy/unique_values"/>
          <call class="Lnumpy/unique_values" name="do" type="virtual" def="values" descriptor="()LRoot;" arg0="values_fn" arg1="ar" numArgs="2"/>
          <new def="index_fn" class="Lnumpy/unique_indices"/>
          <call class="Lnumpy/unique_indices" name="do" type="virtual" def="index" descriptor="()LRoot;" arg0="index_fn" numArgs="1"/>
          <new def="inverse_fn" class="Lnumpy/unique_indices"/>
          <call class="Lnumpy/unique_indices" name="do" type="virtual" def="inverse" descriptor="()LRoot;" arg0="inverse_fn" numArgs="1"/>
          <new def="counts_fn" class="Lnumpy/unique_indices"/>
          <call class="Lnumpy/unique_indices" name="do" type="virtual" def="counts" descriptor="()LRoot;" arg0="counts_fn" numArgs="1"/>
          <new def="ret" class="Ltuple"/>
          <putfield class="LRoot" field="0" fieldType="LRoot" ref="ret" value="values"/>
          <putfield class="LRoot" field="1" fieldType="LRoot" ref="ret" value="index"/>
          <putfield class="LRoot" field="2" fieldType="LRoot" ref="ret" value="inverse"/>
          <putfield class="LRoot" field="3" fieldType="LRoot" ref="ret" value="counts"/>
          <return value="ret"/>
        </method>
      </class>
      <!-- Slot 0 of `np.unique`: the unique values, dtype preserved from `ar`, flattened to rank 1 with a statically unresolvable length. wala/ML#799. -->
      <class name="unique_values" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="2" paramNames="self ar">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <!-- Slots 1-3 of `np.unique`: the index, inverse, and counts outputs, always int64 (positional indices), rank 1 with a statically unresolvable length. wala/ML#799. -->
      <class name="unique_indices" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="1" paramNames="self">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <!-- https://numpy.org/doc/stable/reference/generated/numpy.reshape.html -->
      <!-- Function-form counterpart of `numpy/ndarray/reshape`: `np.reshape(x, shape)` rather than `x.reshape(shape)`. Modeled as a class-per-function so that `np.reshape` resolves to this class via a field lookup on the numpy module, and the invocation dispatches to its `do` method. Shape comes from arg 1 (target shape); dtype is preserved from arg 0 (input tensor); see `NpReshape`. -->
      <class name="reshape" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="3" paramNames="self x shape">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <!-- https://numpy.org/doc/stable/reference/generated/numpy.transpose.html. `np.transpose(a, axes)` permutes `a`'s dimensions by the constant `axes`, defaulting to full reversal; `NpTranspose` computes the output shape and passes the dtype through (wala/ML#835). Result is a fresh ndarray with the method fields re-wired, like `reshape`. -->
      <class name="transpose" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="3" paramNames="self a axes">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
    </package>
    <package name="numpy/random">
      <!-- The legacy module-level random surface. Every draw returns a `float64` array of the requested shape, except that a draw with no shape request returns a Python scalar instead; the shape request is spelled as variadic dimensions here and as a `size` argument in the `normal`/`uniform` family, which is why the two shapes of signature take different generators (`NpRandomVariadicDraw` and `NpRandomSizedDraw`). Both allocate the same `Lnumpy/ndarray` and let the generator decide which case a given call is. wala/ML#827. -->
      <!-- https://numpy.org/doc/stable/reference/random/generated/numpy.random.randn.html -->
      <!-- `np.random.randn(d0, d1, ...)`: standard-normal draws, dimensions given variadically. The rank comes from the call's own arity rather than from the parameter list below, so a call passing more dimensions than are modeled here keeps its rank; what it can lose is the individual sizes, which then read as unknown fixed axes. -->
      <class name="randn" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="7" paramNames="self d0 d1 d2 d3 d4 d5">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <!-- https://numpy.org/doc/stable/reference/random/generated/numpy.random.rand.html -->
      <!-- `np.random.rand(d0, d1, ...)`: uniform draws over [0, 1), variadic like `randn`. -->
      <class name="rand" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="7" paramNames="self d0 d1 d2 d3 d4 d5">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <!-- https://numpy.org/doc/stable/reference/random/generated/numpy.random.normal.html -->
      <!-- `np.random.normal(loc, scale, size)`: the shape is the `size` argument, so the distribution parameters occupy the two positions before it. -->
      <class name="normal" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="4" paramNames="self loc scale size">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <!-- https://numpy.org/doc/stable/reference/random/legacy.html#numpy.random.RandomState -->
      <!-- `np.random.RandomState(seed)`: the legacy GENERATOR object. Its draw methods are the same functions the module-level surface exposes, so the instance carries the same per-function fields rather than duplicating their models; `rng.uniform(size=...)` then resolves exactly as `np.random.uniform(size=...)` does. Without this the generator is unmodeled, every draw taken through it is shape-absent, and a following `.astype(...)` recovers the dtype while leaving the shape unknown, which is the shape-absent-with-dtype signature. wala/ML#827. -->
      <class name="RandomState" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="2" paramNames="self seed">
          <new def="rs" class="Lobject"/>
          <new def="rs_uniform" class="Lnumpy/random/uniform"/>
          <putfield class="LRoot" field="uniform" fieldType="LRoot" ref="rs" value="rs_uniform"/>
          <new def="rs_normal" class="Lnumpy/random/normal"/>
          <putfield class="LRoot" field="normal" fieldType="LRoot" ref="rs" value="rs_normal"/>
          <new def="rs_randn" class="Lnumpy/random/randn"/>
          <putfield class="LRoot" field="randn" fieldType="LRoot" ref="rs" value="rs_randn"/>
          <new def="rs_rand" class="Lnumpy/random/rand"/>
          <putfield class="LRoot" field="rand" fieldType="LRoot" ref="rs" value="rs_rand"/>
          <new def="rs_permutation" class="Lnumpy/random/permutation"/>
          <putfield class="LRoot" field="permutation" fieldType="LRoot" ref="rs" value="rs_permutation"/>
          <new def="rs_seed" class="Lnumpy/random/seed"/>
          <putfield class="LRoot" field="seed" fieldType="LRoot" ref="rs" value="rs_seed"/>
          <return value="rs"/>
        </method>
      </class>
      <!-- https://numpy.org/doc/stable/reference/random/generated/numpy.random.permutation.html -->
      <!-- `np.random.permutation(x)`: shuffles along the FIRST axis only, so an array argument's shape survives it unchanged, exactly as `ndarray.astype` preserves its receiver's. An integer argument is the other arm: `permutation(n)` is a shuffled `arange(n)`, rank one of length `n` and dtype `int64`. `NpPermutation` distinguishes the two, since passing an integer's scalar shape through would report a scalar where the runtime gives a vector (wala/ML#858). -->
      <class name="permutation" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="2" paramNames="self x">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <!-- https://numpy.org/doc/stable/reference/random/generated/numpy.random.uniform.html -->
      <!-- `np.random.uniform(low, high, size)`: the `normal` signature with different distribution parameters. -->
      <class name="uniform" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="4" paramNames="self low high size">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <!-- https://numpy.org/doc/stable/reference/random/generated/numpy.random.seed.html -->
      <!-- `np.random.seed(seed)` mutates the global generator state and returns `None`. Modeled so that the call resolves to a target and its result is `None` rather than an unmodeled value; there is no generator, so the result is not a tensor source. -->
      <class name="seed" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="2" paramNames="self seed">
          <return value="null"/>
        </method>
      </class>
    </package>
    <package name="numpy/ndarray">
      <!-- NOTE: This block of ndarray method-field populations (`astype`, `reshape`, ...) is duplicated intentionally in every ndarray-producing method's `do()` (both here and in the mnist `x_train`/`y_train`/`x_test`/`y_test` outputs in `tensorflow.xml`). Because WALA uses 1-CFA context sensitivity for general methods, if we were to centralise this into a single shared helper method, 1-CFA would merge all calls to that helper and cause massive aliasing — every ndarray in the program would share internal field values, losing precision for shapes/dtypes in chained operations. Inlining the field populations at each endpoint's `do()` gives each ndarray allocation a unique site and preserves call-site context. Mirrors the Dataset-method pattern in `tensorflow.xml`; if WALA ever lets us declare class-level field summaries once without the 1-CFA collision, this duplication should be consolidated. Tracking upstream: wala/WALA#1890. -->
      <!-- https://numpy.org/doc/stable/reference/generated/numpy.ndarray.astype.html -->
      <!-- Modeled as a class-per-function (same pattern as `tensorflow/data/shuffle`, etc.) so that attribute access `x.astype` on an ndarray-like value resolves to this class via a field lookup, and the subsequent invocation dispatches to its `do` method. -->
      <!-- The method form binds only `dtype` (position 0 after `self`): the array being narrowed is the RECEIVER, which the trampoline never passes as an argument — `AstypeOperation` reads it off the property read that defined the invoke's function object, the same way `numpy/ndarray/transpose` does (wala/ML#796). A `self ndarray dtype` layout binds the user's dtype into the phantom array slot and leaves the named `dtype` slot empty, so a name-keyed read finds nothing and the dtype degrades silently; with a float32 default in front of it, that reads as a confident float32 for every narrowing target (wala/ML#849). -->
      <class name="astype" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="2" paramNames="self dtype">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <!-- https://numpy.org/doc/stable/reference/generated/numpy.ndarray.reshape.html -->
      <class name="reshape" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="3" paramNames="self ndarray shape">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
      <!-- https://numpy.org/doc/stable/reference/generated/numpy.ndarray.tolist.html -->
      <!-- `x.tolist()` returns the array's elements as a nested Python list. The result is a distinct allocation (`tolist_result`, not `Lnumpy/ndarray`) so producer delegation dispatches `TolistOperation` (dtype and shape recovered from the receiver) without conflating the value with a real ndarray; the downstream `np.array(list)` rebase reads through it. wala/ML#796. -->
      <class name="tolist" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="2" paramNames="self ndarray">
          <new def="ret" class="Lnumpy/ndarray/tolist_result"/>
          <return value="ret"/>
        </method>
      </class>
      <!-- Allocation-only marker class for `tolist` results; no method body (same pattern as `ndarray`). -->
      <class name="tolist_result" allocatable="true"></class>
      <!-- https://numpy.org/doc/stable/reference/generated/numpy.ndarray.transpose.html. The method form of `np.transpose` (wala/ML#835); the trampoline supplies the receiver as `a`, so `NpTranspose` serves both forms. -->
      <!-- The method form binds only `axes` (position 0 after `self`): the input array is the RECEIVER, which the trampoline never passes as an argument — `NpTranspose`'s method form reads it off the property read that defined the invoke's function object (wala/ML#796). A `self a axes` layout here would bind a user's positional `axes` into the input slot. -->
      <class name="transpose" allocatable="true">
        <method name="do" descriptor="()LRoot;" numArgs="2" paramNames="self axes">
          <new def="ret" class="Lnumpy/ndarray"/>
          <new def="astype_fn" class="Lnumpy/ndarray/astype"/>
          <putfield class="LRoot" field="astype" fieldType="LRoot" ref="ret" value="astype_fn"/>
          <new def="tolist_fn" class="Lnumpy/ndarray/tolist"/>
          <putfield class="LRoot" field="tolist" fieldType="LRoot" ref="ret" value="tolist_fn"/>
          <new def="reshape_fn" class="Lnumpy/ndarray/reshape"/>
          <putfield class="LRoot" field="reshape" fieldType="LRoot" ref="ret" value="reshape_fn"/>
          <new def="transpose_fn" class="Lnumpy/ndarray/transpose"/>
          <putfield class="LRoot" field="transpose" fieldType="LRoot" ref="ret" value="transpose_fn"/>
          <return value="ret"/>
        </method>
      </class>
    </package>
  </classloader>
</summary-spec>
