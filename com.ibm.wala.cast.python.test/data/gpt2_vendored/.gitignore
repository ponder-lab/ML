# The vendored gpt-2 driver reads `dummy.tfrecord` and nothing writes it, so it is an input
# rather than a byproduct and is committed beside the fixture it feeds. Left here as a note
# because it was briefly ignored as a byproduct, which made the fixture unrunnable from a
# fresh clone.
