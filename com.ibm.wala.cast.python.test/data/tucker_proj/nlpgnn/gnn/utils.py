# Bespoke reachable slice of `kyzhouhzau/NLPGNN`'s `nlpgnn/gnn/utils.py`: the TuckER loader path
# uses no names from it; the module exists so `from nlpgnn.gnn.utils import *` in
# `graphloader.py` resolves.
