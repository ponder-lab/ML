# Spike module for wala/ML#868 layer two (not for commit): classes referred to
# through the module import, mirroring the subject's dotted decorator arguments.
class A:
    def tag(self):
        return 1


class B:
    def tag(self):
        return 2
