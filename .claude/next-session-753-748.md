Implement the **wala/ML#753 + #748 fixpoint-monotonicity arc** (with wala/ML#756 as the entry step): make the worklist engine's settled states evaluation-order-independent, at member granularity (#753) and axis granularity (#748).

**Read first:** #753's body and its scoping comment (the candidate-fix analysis), #748's body, and the memory notes `project_706_parameter_arm.md` and `project_session_state_jul18.md`. The engine is `WorklistTypeResolver.java` (~440 lines, `com.ibm.wala.cast.python.ml/...client/`); the value record is `ShapeResult`.

**The mechanism (already diagnosed, verify then build):** `evaluate` joins every recomputation into state (`join(old, result)`), and `ShapeResult.union` ORs `hasUnknown` — so the mark is permanent. On a cyclic demand chain (the BERT encoder loop: `reshape_from_matrix`'s target walks back through `Transformer.call`'s own `get_shape_list` argument), the first evaluation that consumed an on-stack interim read collapses to `unknown`, that mark joins in, and no later re-evaluation retracts it. The acyclic region is deterministic (hybrid inline scheduling = reverse-topological); only cycle-internal chaotic order (identity-hash-seeded) varies, which is why outcomes flip with JVM run history. Witnesses: `Transformer.call`'s `input_tensor` gains/loses a `? of float32` member across identical reruns (#753); the gpt-2 decoder chain's first-evaluation `(2, 3, 8)` members degrade to `(Dynamic, Unresolved, ...)` in final states (#748, `EWO record broadcast` FINE logs).

**Deliverable order:**

1. **Cycle-order perturbation knob FIRST (wala/ML#756, filed for exactly this)** — `ariadne.typeResolution.reverseSeeds` does NOT reproduce the flake (verified: seed order only permutes demand roots; the multiplicity is inside the cycle's iteration order, unreached by any current knob). Add a seeded shuffle of the worklist poll order and the `dependents` re-enqueue order, mirroring the reverseSeeds property pattern. Success = a deterministic in-suite reproducer: the `Transformer.call` probe flips with the knob, in one JVM, no test pairing needed. Extend `WorklistOrderInvarianceTest` with it per #756 (the reversed-seeds-only hole is why it stayed green while #753 flaked); closing #756 unblocks #753's verification (the dependency is recorded via blocked_by). Probe recipe: a temporary test on `nlpgnn_full_proj`, FUT `Transformer.call` (`nlpgnn/layers/transformer.py`), params 2/17, expectation `Map.of(3, Set.of(), 4, Set.of())`, then read the `Tensor variables for` dump from the surefire XML.
2. **The fix.** Recommended design (option 2 of the #753 scoping comment): a **post-fixpoint canonicalization pass** — after `solve()` stabilizes, recompute each query once in reverse-topological order over the SCC condensation, REPLACING values (join only within an SCC). This covers both #753 (the permanent mark) and #748 (interim per-axis folds riding into final state) in one design. Smaller fallback if canonicalization destabilizes: option 1, a retractable unknown mark on any complete re-evaluation (one that consumed no on-stack read — track one boolean per evaluating frame). Beware: transfers are NOT monotone (`members().isEmpty()` collapses), the evaluation-count valve pins oscillators at 100, and `promotePureCycles` interacts with anything that changes cycle-bottom semantics.
3. **Re-add the in-vivo pin PR 629 had to drop:** `testNlpgnnFullTransformerInput` on `Transformer.call` — the expected 16-member vn3 union and 6-member vn4 union are recorded verbatim in `project_706_parameter_arm.md`. It must hold under the perturbation knob and under test pairing (`testNlpgnnFullInteractive` first in the same JVM was the most reliable reproducer, 3/3; the `testNlpgnnFullGeneration` pairing flipped between runs).
4. **Check the #748 witnesses** (gpt-2 decoder `(2,3,8)` members) and update its pins; close both issues if the canonicalization covers them, else split residuals per the parent-or-independent rule.
5. Full suite; watch `WorklistOrderInvarianceTest` and `DiagnosticLoggingVolumeTest` (both now required CI shards). A side-benefit worth measuring: fewer cycle re-evaluations may shrink the `whole-project-fixtures (nlpgnn)` CI leg (currently 6.5 min, the queue ceiling).
6. When green and pinned, cut an Ariadne release per `RELEASING.md` (one-click Cut release workflow; current dev version 0.52.38-SNAPSHOT).

**Constraints:** branch off master at start; the encoder-loop exact-set pin stays OUT of the suite until the fix lands (the do-not-pin rule in memory); `ARIADNE_WORKLIST_DUMP`/`ariadne.typeResolution.dumpFilter` exists for engine-state dumps; engine `ShapeQuery` keys include `exact`, so "computed once, read null" can be two different keys. Don't touch other repos.
